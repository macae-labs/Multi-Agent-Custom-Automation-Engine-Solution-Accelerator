{
  "azureFunctions.projectSubpath": "src/backend/azure_functions",
  // El venv artesanal de la raíz fue eliminado (todo entorno proviene de un
  // contrato). El entorno canónico para tests/análisis es el del backend.
  // NOTE: Pylance analysis (extraPaths, venv) is governed by the root
  // pyrightconfig.json — do not duplicate python.analysis.* here.
  // This path only feeds the Python extension (run/debug/pytest discovery).
  "python.defaultInterpreterPath": "${workspaceFolder}/src/backend/.venv/bin/python",
  "python.terminal.activateEnvironment": true,

  // ── Testing: pytest auto-discovery ────────────────────────────────
  // pytest.ini `testpaths` already scopes to src/tests/backend + src/tests/agents.
  // Integration tests need real Azure infra → deselected by default.
  "python.testing.pytestEnabled": true,
  "python.testing.unittestEnabled": false,
  "python.testing.pytestArgs": ["--no-header", "-q", "-m", "not integration"],

  // ── mypy en el editor: determinista, misma vista que el gate ──────
  // Sin cwd, la extensión corre mypy desde donde caiga y los tests
  // (src/tests, sin pyproject en su ruta) terminan con Config File:
  // Default. Anclado a src/backend usa el mismo pyproject que
  // `uv run mypy .` (mypy_path con $MYPY_CONFIG_FILE_DIR + overrides).
  "mypy-type-checker.cwd": "${workspaceFolder}/src/backend",

  "[python]": {
    "editor.defaultFormatter": "ms-python.autopep8",
    "editor.formatOnSave": true
  },

  "editor.codeActionsOnSave": {},

  "files.trimTrailingWhitespace": true,
  "files.insertFinalNewline": true,
  "files.trimFinalNewlines": false,
  "kiroAgent.configureMCP": "Disabled",
  "azureFunctions.deploySubpath": "src/backend/azure_functions",
  "azureFunctions.scmDoBuildDuringDeployment": true,
  "azureFunctions.pythonVenv": ".venv",
  "azureFunctions.projectLanguage": "Python",
  "azureFunctions.projectRuntime": "~4"
}

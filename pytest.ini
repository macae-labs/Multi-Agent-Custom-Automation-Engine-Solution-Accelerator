[pytest]
addopts = -p pytest_asyncio --import-mode=importlib
testpaths =
    src/tests/backend
    src/tests/agents
markers =
    integration: marks tests as integration tests requiring real Azure infrastructure (deselect with '-m "not integration"')

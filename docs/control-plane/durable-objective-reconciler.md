# Plano de control durable: objetivo persistente y reconciliación

Estado: diseño confirmado (2026-09-15). Incremento 1 implementado. Incremento 2, clarificación, implementado (2026-09-16): `ProxyAgent.run` emite el `Content` marcado con el ancla en `session.state`; `run_orchestration` aparca en `request_info` (`_park_on_request_info`: `waiting_for` durable en el plan, `USER_CLARIFICATION_REQUEST` a la UI, el BackgroundTask termina); `resume_orchestration` restaura el checkpoint con la respuesta; `user_clarification` y el carril de chat reanudan por el helper común del router (equipo del plan, una corrida por sesión, orquestación reutilizada o reconstruida); `_clarification_events`, `set_clarification_pending`, `wait_for_clarification`, `cleanup_clarification`, `get_pending_clarification_for_session` y `clarification_timeout` no existen, y un test lo afirma con cero ocurrencias en `src/backend`. Aprobación por `enable_plan_review`, implementada (2026-09-16) con el mismo mecanismo: `_park_on_request_info` discrimina `MagenticPlanReviewRequest` y manda `PLAN_APPROVAL_REQUEST` con el `MPlan` guardado en `waiting_for`; `plan_approval` discrimina por `PlanApprovalResponse.decision` (`approve | revise | reject`, único discriminador, mismo contrato en ambos lados; `approved` retirado del dataclass, de los tipos y de las llamadas del frontend): `approve` reanuda con `MagenticPlanReviewResponse.approve()`; `revise` exige `feedback` (400 si falta) y reanuda con `.revise(feedback)`, el manager replanifica, regenera el `MPlan` y la UI recibe un `PLAN_APPROVAL_REQUEST` nuevo, como midió el harness; `reject` cancela el aparcado (`cancel_parked`, plan `canceled`, `FINAL_RESULT_MESSAGE`). `handle_plan_approval` sólo marca aprobado en `approve` y lee únicamente el `Plan` persistido; `orchestration_config.plans` no existe. La UI tiene el control de revisión en `StreamingPlanResponse` (textarea, manda `revise` con el texto). `HumanApprovalMagenticManager.plan()` ya no espera; `approvals`, `_approval_events`, `approval_timeout`, `set_approval_pending`, `set_approval_result`, `wait_for_approval` y `cleanup_approval` no existen, y el test de cero ocurrencias cubre ambos conjuntos de nombres. Residual declarado: los checkpoints de `workflow_checkpoints` no se borran al terminar ni al cancelar un plan; la retención la ejecuta el reconciliador del incremento 3 al plegar el work item a estado terminal, borrando la cadena por `previous_checkpoint_id`. Pendientes: incrementos 3 y 4, sonda de canary para sesiones del servicio.

## Por qué

Hoy la unidad de ejecución es el turno. Un request entra, el Model Router razona, llama tools, reintenta, responde, y nadie posee el objetivo después de la respuesta. Model Router, Magentic, agentes Foundry, MCP, Workspace, GitHub, Cosmos, telemetría y sondas son capacidades esperando una llamada. Lo que falta es el dueño del objetivo: un trabajo que sobrevive al turno, que decide qué evento lo despierta y que continúa hasta su condición terminal o hasta su techo de autoridad. El ciclo observar, detectar, diagnosticar, proponer, actuar, validar, comparar, aprender y REARMAR se hace hoy a mano.

## Hechos verificados (HEAD a47b4724)

- `run_orchestration` corre como `BackgroundTask` (`v4/api/router.py:869`): sobrevive a la respuesta HTTP, no al proceso.
- `wait_for_approval` es `asyncio.wait_for(event.wait(), timeout)` sobre `asyncio.Event` en memoria (`v4/config/settings.py:161`). La espera humana no es un estado: es un hilo bloqueado con reloj. Un reinicio o el timeout la matan. Es lo opuesto de REARM por identidad y causalidad.
- `init_orchestration` pasaba `InMemoryCheckpointStorage()` al `MagenticBuilder` (`orchestration_manager.py:321`, hoy `get_checkpoint_storage()`): los checkpoints morían con el proceso.
- Framework: `Workflow.run(message | responses, checkpoint_id, checkpoint_storage)`. `checkpoint_id` y `responses` en una sola llamada significan "restaurar y luego entregar respuestas". Un `WorkflowCheckpoint` guarda `workflow_name`, `graph_signature_hash`, `checkpoint_id`, `previous_checkpoint_id`, `timestamp`, `messages`, `state`, `pending_request_info_events`, `iteration_count`, `metadata`, `version`. Se crea al final de cada superstep.
- Precondición de reanudación, impuesta por el framework (`agent_framework/_workflows/_runner.py:275`): si `graph_signature_hash` del grafo reconstruido no coincide con el del checkpoint, `WorkflowCheckpointException("Workflow graph has changed since the checkpoint was created")`. Medido: el hash depende de los ids de los executors (los nombres de los participantes) y de las aristas; no depende del nombre del workflow. Dos builds del mismo grafo dan el mismo hash; renombrar un participante lo cambia.
- Nombre del workflow: `MagenticBuilder` no lo fija, así que cada build recibe `WorkflowBuilder-<uuid>` nuevo. La partición `/workflow_name` del contenedor de checkpoints es por build, no por corrida lógica. La identidad de la corrida la lleva el work item: `checkpoint_id` y la cadena `previous_checkpoint_id`.
- HITL nativo del framework: `MagenticBuilder(enable_plan_review=True)` hace que el orquestador llame `ctx.request_info(MagenticPlanReviewRequest(plan, current_progress, is_stalled), MagenticPlanReviewResponse)` (`_magentic.py:1030`). El workflow queda inactivo con la petición pendiente, el checkpoint conserva `pending_request_info_events`, y se continúa con `run(checkpoint_id=…, responses={request_id: MagenticPlanReviewResponse.approve() | .revise(feedback)})`. El producto no usa esto: `HumanApprovalMagenticManager.plan()` bloquea dentro del executor esperando `wait_for_approval`, por lo que ningún superstep termina y no existe checkpoint durante la espera.
- Credencial de Cosmos: cuatro rutas tenían cada una su copia del mismo `if` con `COSMOSDB_KEY`; tres fabricaban una credencial async propia por servicio y `database_factory` pasaba la síncrona cacheada al cliente aio. Medido con la sonda de INC-2026-005: azure-cosmos 4.9.0 envuelve la credencial síncrona y `get_token` corre en el hilo del loop, 803 ms de congelación frente a 10 ms con la async a igual latencia. Las cuatro rutas resuelven hoy con `config.get_cosmos_credential_async()`, prestada.
- `incident.v1` ya es consumible por máquina: `learn.executable_probe` trae comando, cwd y señales esperadas; `expires_if_not_reverified_by` trae la condición de revalidación; `authority_ceiling` trae el techo. Nada lo consume todavía.

## Modelo

`work_item` (Cosmos, `data_type` nuevo, partición por `user_id` como el resto):

| Campo | Contenido |
|---|---|
| `objective` | Texto del objetivo y `desired_state` verificable |
| `invariants` | Referencias a INC (`INC-2026-00N`) cuyas sondas definen "sano" |
| `evidence` | Referencias a sondas ejecutadas, logs, checkpoints, PRs |
| `pending_actions` | Acciones con clase (`read-only`, `write-scratch`, `write-shared`) |
| `authority_ceiling` | Igual que en `incident.v1` |
| `team_id`, `team_config_hash` | Identidad del equipo con el que se construyó el grafo |
| `checkpoint_id` | Último checkpoint durable de la corrida Magentic |
| `waiting_for` | El REARM: `{kind, identity, fallback_timer}` |
| `status` | `running`, `waiting_for_event`, `waiting_for_human`, `blocked`, `done` |

`work_event` (append-only): `work_item_id`, `kind`, `identity`, `payload`, `evidence`, `timestamp`. Kinds: `github_push`, `ci_run`, `revision_change`, `approval`, `incident_expiry`, `tool_failure`, `timer_fallback`, `reconciled`. El estado del work item es el pliegue de su historial de eventos, que es el principio de Temporal sin traer Temporal.

`waiting_for` se define por identidad y causalidad: un SHA, un `run_id` de CI, una revisión, un `plan_id`, un `request_id`, un `incident_id`. El reloj existe sólo como respaldo de un evento que no llegó.

Identidad del `work_event`: es la causa, nunca la entrega ni su contenido. `id = f"{kind}:{identity}"`, construido en una sola función del producto (`EventStore`), no por cada llamador; el harness la usa en cuanto exista en lugar del literal. `identity` por `kind`: `plan_review` y `clarification`, el `request_id` del `request_info` (una decisión por petición: un `revise` produce otra petición con otro `request_id`, así que su decisión es otro evento; dos decisiones sobre la misma petición chocan, y eso es lo correcto; la decisión y el texto son payload); `incident_expiry`, `incident_id` más la fecha `expires_if_not_reverified_by` que venció (cada vencimiento es una ocurrencia distinta; no es reloj, es el valor que define la ocurrencia); `github_push`, el SHA; `ci_run`, el `run_id`; `revision_change`, el nombre de la revisión; `tool_failure`, `turn_id` más el id de la llamada; `timer_fallback`, el `request_id` de la espera que respalda. En Cosmos el `id` es único dentro de la partición, no del contenedor: la partición se deriva del propio evento, `pk = kind`, para que la segunda entrega choque siempre sin depender de quién entrega. El orden de aplicación es por `_ts` de llegada (orden de arribo, no identidad); la idempotencia la da el `id`. Con eso `EventStore.append(kind, identity, payload)` es una sola llamada a `create_item` que devuelve `duplicate=True` en el 409, sin leer antes.

## Ciclo

| Primitiva | Pieza existente que la realiza |
|---|---|
| `observe()` | Ejecuta `learn.executable_probe` del INC pertinente; consulta `exceptions` y `dependencies` en Log Analytics |
| `retrieve_relevant_history()` | Búsqueda por `retrieval_keys` y `structural_match` en `docs/incidents/*.json` más el historial del work item |
| `choose_action()` | Model Router o Magentic con el work item como tarea; el plano de ejecución no cambia |
| `check_authority()` | Compara la clase de la acción con `max_action_class_without_human`; si la excede, persiste `waiting_for_human` |
| `execute()` | Tools existentes: MCP, `workspace_exec`, GitHub, sondas |
| `verify()` | Escalera L0 a L7 con control negativo, como exige el schema |
| `persist()` | Apila `work_event` y guarda el checkpoint |
| `rearm()` | Escribe `waiting_for` |

Los INC enriquecen lo que el ciclo recupera; no modifican el motor. Un incidente nuevo es una entrada más en el registro, no una regla más en el código.

## Entrada de eventos y loop

Un único endpoint `POST /api/v4/events` recibe todo: webhooks de GitHub, cambios de revisión, alertas de App Insights, aprobaciones humanas, vencimientos de INC, fallos de tool. Valida, apila el `work_event` y despierta al reconciliador. La durabilidad está en el evento persistido, no en la señal en memoria.

El reconciliador vive en el `lifespan` del backend con un lease en Cosmos (documento con etag; el backend corre con una réplica). Cada iteración carga los work items no terminales cuyo `waiting_for` coincide con el evento recibido, ejecuta el ciclo, persiste y rearma. Termina cuando alcanza un estado estable o su techo de autoridad.

## Precondición de reanudación, explícita

Antes de `run(checkpoint_id=…)` el reconciliador compara `team_id` y `team_config_hash` del work item con la team-config actual. El hash es canónico: nombres y orden de participantes más `deployment_name`. Si no coinciden, no reanuda: replanifica desde el último estado conocido, con un workflow nuevo y el contexto sembrado desde `messages` del checkpoint. El framework valida además `graph_signature_hash`; el work item verifica antes, por identidad de equipo, para no depender de la excepción. Sin esto el incremento 1 funciona hasta el primer cambio de equipo.

## Aprobación como estado, no como espera

La aprobación es un `work_event` de kind `approval` con `request_id` y `plan_id`. Con `enable_plan_review=True` el orquestador emite `request_info`; `run_orchestration`, al ver ese evento, persiste `waiting_for = {kind: plan_review, request_id, checkpoint_id, m_plan}` y termina, liberando el `BackgroundTask`. La aprobación entra por `/api/v4/events` y el reconciliador reanuda con `run(checkpoint_id, responses={request_id: approve() | revise(feedback)})`. `wait_for_approval`, `set_approval_pending` y `_approval_events` dejan de existir. El endpoint actual de aprobación del plan pasa a producir el `work_event`; el contrato hacia la UI no cambia. Respaldar `approvals` con un documento manteniendo el `asyncio.Event` seguiría siendo un hilo esperando, sólo que con un documento al lado.

## Incrementos y verificación

1. `CosmosCheckpointStorage`. Hecho: `common/services/checkpoint_storage.py`, seis métodos del protocolo, contenedor propio `workflow_checkpoints`, partición `/workflow_name`, misma codificación que `FileCheckpointStorage`. Sustituye la línea 321 sin cambio de contrato hacia afuera; el cliente se cierra en el `lifespan`. La credencial es prestada: `config.get_cosmos_credential_async()` devuelve `COSMOSDB_KEY` en dev y, si no, la credencial compartida del proceso, que sólo `aclose_shared_resources` cierra. Ese resolutor único reemplazó las tres copias del mismo `if` que fabricaban una credencial propia por servicio (`chat_cosmos_service`, `mcp_connections_service`, el storage), el patrón de `shared-credential-kill`. Verificado con `src/tests/backend/common/services/test_checkpoint_storage.py` contra el framework real: los checkpoints persisten en el contenedor, una instancia nueva del mismo grafo reanuda por `checkpoint_id`, y un grafo distinto es rechazado por el framework. Pendiente y de clase write-shared: la primera ejecución contra Cosmos de producción crea el contenedor, y el E2E de matar el proceso a mitad de un plan y reanudar por `checkpoint_id` escribe en Cosmos prod. Ambos con aprobación previa.
2. Aprobación como evento. Medido antes de tocar el producto con `src/tests/backend/v4/orchestration/test_plan_review_durable_harness.py`: un `ScriptedChatClient` del framework (sin Foundry) alimenta un `StandardMagenticManager` real con `enable_plan_review=True` sobre `CosmosCheckpointStorage`. El orquestador emite `request_info` con `MagenticPlanReviewRequest`, el superstep termina y el checkpoint conserva la petición pendiente con su `request_id`; una instancia nueva del mismo grafo hace `run(checkpoint_id, responses={request_id: approve()})` y llega a la respuesta final escribiendo más checkpoints; `revise(feedback)` replanifica y emite una petición nueva con otro `request_id`; un participante renombrado es rechazado. Lo que sobrevive de `HumanApprovalMagenticManager`: `_apply_pending_history` y `seed_chat_history` (siembra del contexto), `create_progress_ledger` (tope de rondas y agentes no consultados), `plan_to_obj` (la conversión a `MPlan` que la UI consume) y `prepare_final_answer`. Lo que se mueve al manejador del evento `request_info` en `run_orchestration`: la conversión del ledger a `MPlan`, el registro del plan, el `PLAN_APPROVAL_REQUEST` por WebSocket y la persistencia de `waiting_for`. Lo que desaparece: `plan()` bloqueante, `_wait_for_user_approval`, `wait_for_approval`, `set_approval_pending`, `_approval_events`, el `TIMEOUT_NOTIFICATION` por reloj. Verificación del incremento: plan con aprobación pendiente, reinicio del backend, aprobación, y la corrida continúa desde el checkpoint; la UI recibe los mismos tipos de mensaje que hoy.

   Clarificación al usuario, misma familia y camino nativo ya medido (`src/tests/backend/v4/orchestration/test_user_input_durable_harness.py`). Hoy `ProxyAgent._wait_for_user_clarification` (`proxy_agent.py:284`) bloquea dentro del executor sobre `orchestration_config.wait_for_clarification`, otro `asyncio.Event` con reloj. El `AgentExecutor` del framework ya tiene el hueco: si `agent.run()` devuelve un `AgentResponse` cuyo `user_input_requests` no está vacío, el executor registra cada petición, llama `ctx.request_info(request, Content)` y devuelve `None`; el workflow queda inactivo con la petición en el checkpoint. Al reanudar con `run(checkpoint_id, responses={request_id: Content})`, `handle_user_input_response` reconstruye la entrada del agente y vuelve a invocarlo. Medido: la petición es un `Content` con `user_input_request=True` (no existe un tipo `user_input_request` en `ContentType`; los del framework son `function_approval_request` y `oauth_consent_request` sobre esa misma marca), su `id` lo pone el agente y el `request_id` del evento lo genera el workflow, son distintos y el work item guarda el segundo; los participantes se invocan en streaming; el agente reanudado recibe instrucción, su propia pregunta y la respuesta humana; `Content.from_text(respuesta)` llega con rol `user` y `Content.from_function_result(...)` con rol `tool`. Con esto el ProxyAgent sigue siendo agente: su `run()` devuelve la pregunta como `Content` marcado en vez de esperar, y la siguiente invocación, con la respuesta humana, es donde continúa. El ProxyAgent no pasa por ningún cliente Foundry al preguntar: es un `BaseAgent` que fabrica sus propios `AgentResponseUpdate` (`proxy_agent.py:221`), y el harness usa un doble con esa misma estructura, `ResponseStream` sobre un generador propio con `AgentResponse.from_updates` como finalizador; el executor detecta el `Content` marcado durante el stream y lo conserva en el checkpoint.

   Ancla de "¿ya pregunté?", medida: un `BaseAgent` recibe en `run()` sólo el `_cache` del executor, y al reanudar ese cache es únicamente el mensaje `user` con la respuesta; la conversación no llega, y la instancia es nueva, así que ni el texto ni un contador sirven. Un `Agent` de cliente de chat sí ve la conversación, pero no por el executor: `Agent.run` añade solo un `InMemoryHistoryProvider` (`_agents.py:1099`) cuando hay sesión sin proveedores, sin `service_session_id` y sin `store`, y ese proveedor guarda el historial en `session.state`. El ProxyAgent no tiene ese mecanismo. La regla determinista de `ProxyAgent.run(messages, session)`: `pending = session.state.get("pending_clarification")` con `{id, question}`; si existe y `messages` trae un `user`, es la respuesta: limpiar el ancla y emitir la continuación; si no, generar `Content(type, text=pregunta, id=nuevo, user_input_request=True)`, guardar el ancla en `session.state` antes de que el stream termine, porque `on_checkpoint_save` serializa la sesión al cerrar el superstep, y emitirlo como `AgentResponseUpdate` en su `ResponseStream`. El harness lo afirma leyendo el objeto restaurado del checkpoint, en `state._executor_state.<participante>.agent_session.state.pending_clarification`, con el mismo `Content.id` que viajó en el `request_info`. Con dos aclaraciones en el mismo plan cada reanudación responde a su propio id.

   `MagenticResetSignal`, medido: `handle_magentic_reset` asigna `self._agent_thread = agent.create_session()`, pero los runs usan `self._session`, que no se toca (agent_framework_orchestrations 1.0.0b260311). La sesión del executor sobrevive al reset y con ella `session.state`: tras estancamiento, reset y replan, el contador de turnos del participante continúa. El ProxyAgent no puede contar con el reset para borrar un ancla: la limpia él al responder y la sobrescribe al preguntar, y como el reset sólo ocurre con el workflow activo, nunca coincide con una pregunta pendiente. `USER_CLARIFICATION_REQUEST` por WebSocket se emite desde el manejador de `request_info` en `run_orchestration`, discriminando por el tipo de `event.data` (`MagenticPlanReviewRequest` o `Content` marcado), y ahí se persiste `waiting_for` con el `request_id` del evento. `user_clarification` no toca ningún `Event`: escribe el `work_event` con `{request_id, Content.from_text(answer)}` y el reconciliador reanuda. En la misma tanda y sin guardas de compatibilidad desaparecen `_wait_for_user_clarification` (`proxy_agent.py:284`), `_clarification_events`, `set_clarification_pending`, `wait_for_clarification` (`settings.py`) y el `TIMEOUT_NOTIFICATION` de clarificación. `USER_CLARIFICATION_RESPONSE` y los cuatro tipos de WebSocket no cambian.

   Sesiones del lado del servicio: `AgentSession.to_dict()` serializa `service_session_id`, el núcleo lo asigna desde `response.conversation_id` (`_agents.py:1232`) y `AzureAIClient` lo devuelve, así que un participante Foundry reanudado se reengancha a la misma conversación del servicio. Lo que el checkpoint no captura son los mensajes que viven en el servidor: el aviso del framework lo dice para `AzureAIAgentClient`, y la conversación local del executor puede ir por detrás de la del servicio. No es medible sin Foundry; su sonda es de canary, de clase write-shared: un plan con un participante Foundry, checkpoint, proceso nuevo, reanudación, y comparar la conversación del servicio con `full_conversation` del checkpoint.
3. `work_item`, `work_event`, `/api/v4/events` y el loop con lease. Lo que ya existe tras el incremento 2: `Plan.waiting_for` es un work item de una sola clase (`clarification` | `plan_review`), `resume_orchestration` y `cancel_parked` son sus dos transiciones, y `_plan_waiting_for` es la consulta por identidad (`request_id`, `m_plan_id`, `plan_id`, sesión). Falta el evento y el dueño: el documento `work_event` append-only; `POST /api/v4/events` que valida, lo escribe y despierta al reconciliador; el loop en el `lifespan` con lease en Cosmos que, por cada evento, localiza el work item por identidad, ejecuta la transición y rearma `waiting_for`. `user_clarification` y `plan_approval` pasan a escribir el evento en lugar de agendar el `BackgroundTask` ellos mismos; el contrato hacia la UI no cambia. La retención de checkpoints entra aquí: al plegar a estado terminal el reconciliador borra la cadena por `previous_checkpoint_id`. Va antes que el 4 porque el 4 corre sobre su loop. Harness antes que producto, como en 1 y 2. Verificación: un evento con identidad produce una y sólo una reconciliación (dos entregas del mismo evento, una transición); un reinicio a mitad de iteración retoma desde el último evento persistido; un plan que llega a terminal no deja checkpoints.
4. Primer controlador: revalidación de INC. `expires_if_not_reverified_by` vencido produce `incident_expiry`; el reconciliador ejecuta `executable_probe.command_or_test`; con señal sana avanza `last_verified`; con señal de fallo pone `status: needs_revalidation` y `learn.operational: false`, que es un invariante del schema, y deja el work item en `waiting_for_human` si el techo lo exige. Es el controlador más barato porque el artefacto ya es ejecutable, y es el que convierte `incident.v1` en estado operacional consumido por máquina. Verificación: un INC vencido en un fixture produce la transición; control negativo con la sonda de INC-2026-004 y `src/tests/backend/auth/__init__.py` restaurado.

## Qué no cambia

Model Router, Magentic, agentes Foundry y MCP siguen siendo el plano de ejecución. Temporal entra sólo si el reconciliador de un proceso deja de bastar por réplicas o volumen.
